# pnpm 11 refuses to finish an install whose dependencies have unapproved build
# scripts, and approval is otherwise per-machine. Declaring it here keeps CI and
# a fresh clone working without anyone running `pnpm approve-builds` by hand.
#
# esbuild's postinstall links the platform binary Vite uses.
allowBuilds:
  esbuild: true
