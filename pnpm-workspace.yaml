# pnpm 11 refuses to finish an install that has unapproved build scripts, so
# the ones this project needs are declared here rather than approved by hand
# on each machine. esbuild's postinstall links the platform binary Vite uses.
onlyBuiltDependencies:
  - esbuild
